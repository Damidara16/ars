#can be called direct by using our api library or by webhooks calling our api directly

#ex.
def createTransactionAndTag(...):
    if *args.is_valid:
        result = requests.POST(url, ...)
        sendTag(result)
        return None

ars.PurchaseTag.create(...)
ars.PurchaseTag.create(...)

Transaction and Tag cr
Item crud
employee crud
store crud
category crd

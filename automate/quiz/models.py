from django.db import models
"""
class Quiz(models.Model):
    g1 = models.ForeignKey('Group', on_delete=models.CASCADE)
    g2 = models.ForeignKey('Group', on_delete=models.CASCADE)
    g3 = models.ForeignKey('Group', on_delete=models.CASCADE)
    g4 = models.ForeignKey('Group', on_delete=models.CASCADE)
    g5 = models.ForeignKey('Group', on_delete=models.CASCADE)
    g6 = models.ForeignKey('Group', on_delete=models.CASCADE)

class Group(models.Model):
    GroupName = models.CharField(max_length=255)
    question = models.CharField(max_length=255)
    answer = models.CharField(max_length=1)


class Question(models.Model):
    Qu = (('store of origin','store of origin'),('item name', 'item name'))

class TakenQuiz(models.Model):
    qs = (('a':'a'),('b','b'),('c','c'),('d','d'),)
    q1 = models.CharField(max_length=1, choices=qs)
    q2 = models.CharField(max_length=1, choices=qs)
    q3 = models.CharField(max_length=1, choices=qs)
    q4 = models.CharField(max_length=1, choices=qs)
    q5 = models.CharField(max_length=1, choices=qs)
    q6 = models.CharField(max_length=1, choices=qs)
"""
